import "./Header.scss";

const Header: React.FC = () => (
    <div className="Header">
        <div className="Logo">
            <img src={ '/img/icons/logo.svg' } alt='logo' />
        </div>
    </div>
);

export default Header;